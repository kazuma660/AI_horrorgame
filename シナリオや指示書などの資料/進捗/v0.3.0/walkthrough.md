# Day 3: エンディング + アセット差し替え + 仕上げ 完了報告

Day 3 の全タスクが完了し、**ゲーム本編がすべて完成**しました！

## 実装内容
1. **エンディング画面の実装 (`app/ending/[id]/page.tsx`)**
   - `BAD END`（真っ赤な恐怖の結末）
   - `TRUE END`（脱出したものの不穏な結末）
   - `HIDDEN END`（謎が深まる結末）
   - それぞれ異なるテキストと色合いで表示し、タイトルに戻るボタンを設置しました。

2. **著作権完全クリアの音声アセット生成**
   - Python の `wave` と `math` ライブラリを使用してゼロから波形データを計算し、`se` および `bgm` フォルダに保存しました。
   - サイン波を用いた着信音、ノイズを利用した足音やドアノブの音など、ホラー感のある不気味なサウンドが鳴ります！

3. **背景画像の生成と適用**
   - 安全な生成AIを用いて、「暗い部屋（スマホの光だけ）」と「覗き穴から見た薄暗い廊下」の画像を作成しました。
   - `lib/scenario.ts` の各ノードに `background` プロパティを追加し、シナリオ進行に合わせて背景が切り替わるようにしました。

## 確認方法
ローカルサーバー（`http://localhost:3000`）を開き、「はじめる」をクリックしてください。
今回からは **音（BGMと効果音）** が鳴り、**背景画像** も表示されます！
（※ブラウザの仕様により、最初の画面で一度画面のどこかをクリックしないと音が鳴り始めないのでご注意ください）

最後までプレイし、エンディング画面が表示されれば完成です！
Vercel へのデプロイは、このディレクトリを GitHub に Push するだけで自動的に完了します。
