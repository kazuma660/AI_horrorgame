# 背景画像追加の実装計画

ご要望いただいた「幽霊女性の画像」と「スマホを見ている状況の背景」を追加します！

## 変更内容 (Proposed Changes)

1. **AIによる画像生成**
   - **ベッドでスマホを見ている画像 (`bed_phone.png`)**: 「深夜の暗い部屋で、一人称視点で布団の中からスマホの画面を見ている様子（画面の青白い光だけが周囲を照らしている）」の画像を生成します。
   - **ドアを叩く不気味な女性 (`peephole_woman.png`)**: 「覗き穴越しに見える、長い黒髪で顔が見えない不気味な女性が立っているホラー調の画像」を生成します。

2. **シナリオへの適用 (`lib/scenario.ts`)**
   - 最初のシーン (`id: 'start'`) の `background` を `bed_phone.png` に変更し、冒頭の状況をよりリアルにします。
   - ドアを激しく叩かれるシーン (`id: 'door_check6'`) の `background` を `peephole_woman.png` に変更し、ドアの向こう側に「いる」恐怖を演出します。

## ユーザーへの確認事項 (Open Questions)

> [!NOTE]
> 画像は私がAI（画像生成機能）を使って自動生成し、そのままプロジェクトに配置します。
> （完全オリジナルなので著作権問題は発生しません）
>
> 幽霊の女性について「目を血走らせている」「もっと幽霊っぽく半透明」など、特定のイメージがあれば教えてください！特になければ、よくある「長い髪の不気味な女性」で生成します。

この計画で画像生成と配置を実行してよろしいでしょうか？
